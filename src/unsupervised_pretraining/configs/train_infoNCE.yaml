hydra:
  run:
    dir: .

defaults:
  - _self_
  - trainer: infonce_trainer.yaml
  - datamodule: stl10_unlabeled_module.yaml
  - callbacks: infonce_train_callbacks.yaml
  - loggers: loggers.yaml

work_dir: ${hydra:runtime.cwd}
data_dir: ${work_dir}/dataset
